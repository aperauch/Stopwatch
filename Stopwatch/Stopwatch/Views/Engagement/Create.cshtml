@model Stopwatch.Models.Engagement

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Engagement</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.StartTime)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StartTime)
            @Html.ValidationMessageFor(model => model.StartTime)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StopTime)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StopTime)
            @Html.ValidationMessageFor(model => model.StopTime)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EngagementHours)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EngagementHours)
            @Html.ValidationMessageFor(model => model.EngagementHours)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
